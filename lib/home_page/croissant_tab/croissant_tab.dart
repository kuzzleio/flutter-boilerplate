import 'package:flutter/material.dart';

class CroissantTab extends StatelessWidget {
  const CroissantTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Text('Not implemented yet');
}
